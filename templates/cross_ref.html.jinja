<div class="box cross-ref border">
    <div>{% set separator = joiner("<br />")
        %}{% for id in char_info.cross_ref.values() %}{{ separator()}}
        {{ id|format_char_cross_ref}}
        {% endfor %}
    </div>
</div>
